# Approved JSON feed endpoints; leave empty until a supported source is arranged.
# Source URLs embedded in snapshots must be public attribution URLs, never secret feed URLs.
STANDINGS_FEED_URL=
STANDINGS_FEED_TOKEN=
MHR_FEED_URL=
MHR_FEED_TOKEN=
# Optional GitHub Actions secret. Do not commit a real hook URL.
NETLIFY_BUILD_HOOK=
# Netlify supplies URL at build time. Override only for a custom production domain.
URL=https://2013-st-louis-knights.netlify.app
